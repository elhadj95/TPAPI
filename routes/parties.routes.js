// Routes des parties (placeholder)
const express = require('express');
const router = express.Router();

router.get('/', (req, res) => res.json({ message: 'Liste des parties (placeholder)' }));

module.exports = router;
